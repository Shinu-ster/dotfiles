# Dotfiles

Dotfiles setup for Neovim, Kitty, Rofi, Bash + Starship, and Git.

---

## Steps

### Step 1: Clone the repository
```bash
git clone https://github.com/Shinu-ster/dotfiles.git ~/dotfiles
cd ~/dotfiles
```

### Step 2: Run Install script
```bash
chmod +x install.sh
./install.sh
```
